program:  ../../puffle/main.py
method: bayes
metric:
  name: Custom_metric
  goal: maximize
early_terminate:
  type: hyperband
  min_iter: 5
parameters:
  epochs:
    min: 1
    max: 5
  batch_size:
    min: 500
    max: 2000
  lr:
    min: 0.0001
    max: 0.1
  optimizer:
    values: ["adam", "sgd"]
  regularization_lambda:
    min: 0.05
    max: 0.9

command:
  - ${env}
  - poetry 
  - run 
  - python
  - ${program}
  - ${args}
  - --dataset
  - income
  - --num_rounds
  - 10
  - --num_client_cpus
  - 1
  - --num_client_gpus
  - 0.05
  - --pool_size
  - 20
  - --sampled_clients 
  - 1.0
  - --sampled_clients_test 
  - 1.0
  - --sampled_clients_validation 
  - 1.0
  - --debug
  - False
  - --base_path
  - ../../../reduced_income_data/
  - --dataset_path 
  - ../../../reduced_income_data/
  - --seed 
  - 41 
  - --wandb 
  - True 
  - --sweep
  - True
  - --training_nodes 
  - 1
  - --validation_nodes 
  - 1
  - --test_nodes 
  - 1
  - --tabular_data 
  - True
  - --update_lambda
  - False
  - --metric
  - disparity
  - --splitted_data_dir
  - federated
  - --ratio_unfair_nodes 
  - 0.5
  - --update_lambda
  - False
  - --regularization_mode
  - fixed
  - --regularization
  - True
  - --target 
  - 0.05
  - --cross_silo
  - True