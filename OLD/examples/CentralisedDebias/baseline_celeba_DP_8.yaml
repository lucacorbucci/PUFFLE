program: test_celeba.py
method: bayes
metric:
  name: Custom_metric
  goal: maximize
early_terminate:
  type: hyperband
  min_iter: 3
parameters:
  batch_size:
    min: 512
    max: 2028
  lr:
    min: 0.0001
    max: 0.1
  optimizer:
    values: ["adam", "sgd"]
  clipping_value:
    min: 1.0    
    max: 20.0
  sweep:
    value: True
  epochs:
    value: 10
  epsilon:
    value: 8
  
command:
  - ${env}
  - poetry 
  - run 
  - python
  - ${program}
  - ${args}